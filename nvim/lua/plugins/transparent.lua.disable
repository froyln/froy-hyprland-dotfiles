return {
  "xiyaowong/transparent.nvim",
  lazy = false,
  config = function()
    require("transparent").setup({
      extra_groups = {
        "NormalFloat", -- Ventanas flotantes
        "NvimTreeNormal", -- Si usas NvimTree
        "NeoTreeNormal", -- Si usas NeoTree
      },
    })
  end,
}
