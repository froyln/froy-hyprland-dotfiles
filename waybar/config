{
    "layer": "top",
    "position": "top",
    "height": 24,
    "output": "DP-1",
    "modules-left": ["hyprland/workspaces","hyprland/window"],
    //"modules-center": ["hyprland/window"],
    "modules-right": ["mpris", "idle_inhibitor", "cpu", "memory", "pulseaudio", "clock"],

    "hyprland/workspaces": {
        "format": "{name}",
        "all-outputs": true,
        "on-click": "activate"
    },

    "clock": {
        "format": "{:%I:%M %p}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
        "format-alt": "{:%d/%m/%Y}"
    },

    "mpris": {
        "format": "{player_icon} {artist} - {title}",
        "format-paused": "{status_icon} <i>{artist} - {title}</i>",
        "player-icons": {
            "default": "▶",
            "spotify": "",
            "firefox": ""
        },
        "status-icons": {
            "paused": "II"
        },
        "max-length": 80
    },

    "cpu": {
        "format": "CPU: {usage}%",
        "interval": 2
    },

    "memory": {
        "format": "RAM: {}%",
        "interval": 2
    },

    "pulseaudio": {
        "format": "VOL: {volume}%",
        "format-muted": "MUTED",
        "on-click": "pavucontrol"
    },

    "idle_inhibitor": {
    "format": "{icon}",
      "format-icons": {
        "activated": "",
        "deactivated": ""
      }
    },
}
